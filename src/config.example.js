// Copy this file to src/config.js and fill in your Supabase project's
// values (Project Settings -> API). The anon key is safe to ship client-side
// -- it's constrained by the Row-Level Security policies in
// supabase/schema.sql, not by secrecy.
export const SUPABASE_URL = '';
export const SUPABASE_ANON_KEY = '';
